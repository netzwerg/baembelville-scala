# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                  controllers.Application.listOffered

# Main pages
GET     /listOffered                       controllers.Application.listOffered
GET     /listWanted                        controllers.Application.listWanted
GET     /createPosting                     controllers.Application.createPosting

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                      controllers.Assets.at(path="/public", file)